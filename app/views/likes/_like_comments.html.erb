<!--La vista utiliza la variable de objeto suministrada a esta vista renderizada para mostrar
la cantidad de Me gusta que tiene el objeto en particular.-->
<span>
<%= object.likes.count %>
<!--Se crea un enlace utilizando la ruta comment_likes_path que se pasa al objeto como parámetro.
Esta ruta anidada se vincula con el método de creación en el controlador Me gusta -->
<%= link_to comment_likes_path(object), class: "text-light", method: :post do %>
<!--El parámetro 'method: :post' en el método link_to . Esto es necesario para crear
un nuevo objeto Me gusta. Generalmente link_to se usa para OBTENER información.-->

<!--¿Le gustó? El método auxiliar que se crea en el archivo application_helper se utiliza para
determinar el color del icono de aprobación.-->
  <% if liked?(subject = object, type = 'comment') %>
      <button class="btn btn-liked size-12"><i class="fas fa-thumbs-up"></i></button>
    <% else %>
      <button class="btn btn-neutral size-12"><i class="fas fa-thumbs-up"></i></button>
  <% end %>
<% end %>
Likes
</span>
